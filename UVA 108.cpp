#include<bits/stdc++.h>
using namespace std;
#define clear_arr memset(arr,'\0',sizeof(arr))
int main()
{
    long long ans;
    int n,i,j,abrr[105][105];
    while(cin>>n)
    {
        clear_abrr;
        for(i=0;i<n;i++)
        {
            for(j=0;j<n;j++)
                cin>>arr[i][j];
        }
    }
}
